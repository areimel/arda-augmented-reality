# ============================================================================
# ARDA AR Project Configuration: Greenscreen Video Demo
# ============================================================================
# This file configures an AR experience for the ARDA platform.
# For more information, see the ProjectData interface in src/types/projectData.ts

# ----------------------------------------------------------------------------
# Project Metadata
# ----------------------------------------------------------------------------
# Display name and description shown in the UI
projectName: Greenscreen Video Demo
projectDescription: Demo of greenscreen/chromakey video compositing with alpha channel support.

# ----------------------------------------------------------------------------
# AR Marker Configuration
# ----------------------------------------------------------------------------
# Marker files are compiled using Mind-AR: https://hiukim.github.io/mind-ar-js-doc/tools/compile/
markerFile: /project-files/common/Generic-Marker.mind    # Compiled .mind file for AR tracking
markerImage: /project-files/common/Generic-Marker.png     # Original marker image for reference/printing

# ----------------------------------------------------------------------------
# Experience Configuration
# ----------------------------------------------------------------------------
# Type of AR content: 'video', 'image', or '3d-object'
experienceType: video

# Path to the main content file (video, image, or 3D model)
displayFile: /project-files/common/greenscreen-cat-test.mp4

# UI preset determines the messaging and behavior
# Options: 'default', 'product', 'artwork'
uiPreset: default

# ----------------------------------------------------------------------------
# Model/Object Settings
# ----------------------------------------------------------------------------
modelSettings:
  # Position orientation: 'wall' (vertical), 'flat' (horizontal), or 'card'
  position: wall

  # Scale preset: 'default' or 'large'
  scale: default

  # Video dimensions (aspect ratio: width/height)
  # These values control the size of the video plane in AR space
  videoWidth: "1.0"
  videoHeight: "1.0"

  # Greenscreen/Chromakey settings
  # Enable chromakey effect to remove green background from video
  isGreenscreen: false

  # RGB color values for chromakey (default: green screen)
  # Format: "R G B" where each value is 0.0-1.0
  chromakeyColor: "0.1 0.9 0.2"

# ----------------------------------------------------------------------------
# Splash Page Configuration
# ----------------------------------------------------------------------------
splashPage:
  # Background image for the splash screen (or null for default)
  backgroundImage: null

# ----------------------------------------------------------------------------
# PDF Download Configuration
# ----------------------------------------------------------------------------
# Printable marker PDFs for different orientations
pdfs:
  available: false
  # wall: /project-files/common/pdf/Generic-Marker-Wall.pdf
  # flat: /project-files/common/pdf/Generic-Marker-Flat.pdf

# ----------------------------------------------------------------------------
# Additional A-Frame Assets (Optional)
# ----------------------------------------------------------------------------
# Extra assets that need to be preloaded for the AR scene
# assets:
#   - type: a-asset-item
#     id: displayModel
#     src: /path/to/asset
